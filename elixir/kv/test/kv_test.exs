defmodule KVTest do
  use ExUnit.Case
  # doctest KV

  test "one plus one" do
    assert 1 + 1 == 2
  end

  test "one plus two" do 
    assert 1 + 2 == 3
  end

  test "one plus three" do
    assert 1 + 3 == 5
  end
end
